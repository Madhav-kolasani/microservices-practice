server:
  port: 7709

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/datajpa_db
    username: root
    password: MySQL@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
